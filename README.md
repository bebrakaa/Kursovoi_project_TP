# Страховое агентство - Курсовая работа

Веб-приложение на C# для управления страховым агентством.

## Модули проекта

Проект разделен на два основных модуля:

### 1. Модуль клиентов

- Управление клиентской базой
- Профили клиентов
- Личные данные
- История операций клиентов
- Верификация документов

### 2. Модуль заказов/договоров

- Управление договорами страхования
- Заявки на договоры
- Регистрация договоров
- Платежи по договорам
- Поиск и фильтрация договоров

## Структура проекта

Проект выполнен по принципам Domain-Driven Design (DDD) и Clean Architecture:

```
InsuranceAgency/
├── src/
│   ├── InsuranceAgency.Domain/          # Доменный слой (сущности, бизнес-правила, value objects)
│   │   ├── Entities/                    # Доменные сущности
│   │   │   ├── User.cs                  # Пользователь системы
│   │   │   ├── Client.cs                # Клиент
│   │   │   ├── Agent.cs                 # Агент
│   │   │   ├── Contract.cs              # Договор страхования
│   │   │   ├── Payment.cs               # Платеж
│   │   │   ├── InsuranceService.cs      # Услуга страхования
│   │   │   ├── OperationHistory.cs      # История операций
│   │   │   ├── ContractApplication.cs   # Заявка на договор
│   │   │   └── DocumentVerification.cs  # Верификация документов
│   │   ├── Enums/                       # Перечисления
│   │   └── ValueObjects/                # Объекты-значения
│   ├── InsuranceAgency.Application/     # Слой приложения (сервисы, DTO, интерфейсы)
│   │   ├── Services/                    # Бизнес-сервисы
│   │   ├── DTOs/                        # Data Transfer Objects
│   │   └── Interfaces/                  # Интерфейсы репозиториев и сервисов
│   ├── InsuranceAgency.Infrastructure/  # Слой инфраструктуры (БД, внешние сервисы)
│   │   ├── Persistence/                 # Entity Framework конфигурации
│   │   ├── Repositories/                # Реализации репозиториев
│   │   └── External/                    # Внешние сервисы (платежный шлюз)
│   ├── InsuranceAgency.Web/            # Web приложение (MVC + API)
│   │   ├── Controllers/                 # Контроллеры (API и MVC)
│   │   ├── Views/                       # Razor представления
│   │   └── Middleware/                  # Промежуточное ПО
│   ├── InsuranceAgency.Worker/         # Фоновые задачи (обработка проблемных договоров)
│   └── InsuranceAgency.Tests/          # Тесты (unit и integration)
```

## Технологии

- **.NET 8.0**
- **ASP.NET Core Web API**
- **Entity Framework Core** (SQL Server)
- **AutoMapper**
- **Swagger/OpenAPI**
- **xUnit** (тестирование)
- **Moq** (моки для тестов)

## Функциональность

Проект реализует веб-приложение для управления страховым агентством с разделением по ролям пользователей. Система разделена на два основных модуля:

### Модули системы:

1. **Модуль клиентов** - управление клиентской базой, профилями, личными данными
2. **Модуль заказов/договоров** - управление договорами страхования, заявками, платежами

### Функциональность по ролям:

#### 🔵 Агент (Agent)

- ✅ **Ведение справочника клиентов**

  - Добавление новых клиентов
  - Редактирование данных клиентов
  - Удаление клиентов
  - Поиск клиентов

- ✅ **Управление договорами**

  - Создание договоров страхования
  - Регистрация договоров
  - Просмотр сведений по договорам
  - Поиск договоров по различным критериям

- ✅ **Верификация документов клиента**

  - Проверка и подтверждение документов
  - Отметка о верификации

- ✅ **История операций клиента**

  - Просмотр истории всех операций клиента
  - Фильтрация по типам операций

- ✅ **Авторизация в системе**

#### 🟢 Клиент (Client)

- ✅ **Оплата страхового договора**

  - Инициация платежей
  - Просмотр статуса платежей
  - История платежей

- ✅ **Управление профилем (личный кабинет)**

  - Просмотр личных данных
  - Редактирование личных данных
  - Удаление аккаунта

- ✅ **История операций**

  - Просмотр истории всех своих операций
  - Фильтрация по типам операций

- ✅ **Создание заявки на страховой договор**

  - Подача заявки на оформление договора
  - Просмотр статуса заявки

- ✅ **Авторизация в системе**

#### 🔴 Администратор (Administrator)

- ✅ **Все функции Агента**
- ✅ **Все функции Клиента**
- ✅ **Ведение справочника услуг страхования**
  - Добавление новых услуг
  - Редактирование услуг
  - Удаление услуг
  - Управление тарифами

### Дополнительные возможности:

- **Автоматический редирект** после регистрации на личную панель
- **Переключение аккаунтов** - возможность выйти и войти под другим аккаунтом
- **Поиск и фильтрация** договоров и клиентов
- **История операций** для всех ролей
- **Верификация документов** (для агентов)
- **Заявки на договоры** (для клиентов)

## Настройка и запуск

### Требования

- .NET 8.0 SDK
- SQL Server (LocalDB или полноценный SQL Server)
- Visual Studio 2022 или VS Code

### Как быстро перевести проекты на .NET 8 (либо наоборот для 10.0 версии)

1. Во всех `.csproj` замените `<TargetFramework>net10.0</TargetFramework>` на `net8.0` для проектов:
   - `src/InsuranceAgency.Domain/InsuranceAgency.Domain.csproj`
   - `src/InsuranceAgency.Application/InsuranceAgency.Application.csproj`
   - `src/InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj`
   - `src/InsuranceAgency.Web/InsuranceAgency.Web.csproj`
   - `src/InsuranceAgency.Worker/InsuranceAgency.Worker.csproj`
   - `src/InsuranceAgency.Tests/InsuranceAgency.Tests.csproj`
2. Пакеты Microsoft/EF/ASP.NET с версий `10.0.0` замените на совместимые `8.0.x` (например, `Microsoft.EntityFrameworkCore.SqlServer` и `Microsoft.AspNetCore.*`).
3. После замены выполните `dotnet restore` и пересоберите решение.

### Установка

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd Kursovik_TP
```

2. Восстановите зависимости:

```bash
dotnet restore
```

## Запуск через командную строку

### Шаг 1: Применение миграций базы данных

Миграции уже созданы в проекте. Для применения миграций к базе данных выполните следующие команды:

**Вариант 1: Из корневой папки проекта**

```bash
# Перейдите в корневую папку проекта (если еще не там)
cd D:\ForVLGU\KURSOVIK_TP\Kursovik_TP

# Примените миграции (из корня проекта)
dotnet ef database update --project src/InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj --startup-project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj
```

**Вариант 2: Из папки Infrastructure**

```bash
# Перейдите в папку Infrastructure
cd src/InsuranceAgency.Infrastructure

# Примените миграции
dotnet ef database update --startup-project ../InsuranceAgency.Web
```

**Вариант 3: Из папки Web (рекомендуется)**

```bash
# Перейдите в папку Web
cd src/InsuranceAgency.Web

# Примените миграции
dotnet ef database update --project ../InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj
```

### Создание новой миграции (если нужно)

Если вы внесли изменения в модели и нужно создать новую миграцию:

```bash
# Из корня проекта
dotnet ef migrations add НазваниеМиграции --project src/InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj --startup-project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj

# Или из папки Infrastructure
cd src/InsuranceAgency.Infrastructure
dotnet ef migrations add НазваниеМиграции --startup-project ../InsuranceAgency.Web
```

### Просмотр списка миграций

```bash
# Из корня проекта
dotnet ef migrations list --project src/InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj --startup-project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj
```

### Откат миграции (если нужно)

```bash
# Откатить последнюю миграцию
dotnet ef database update ПредыдущаяМиграция --project src/InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj --startup-project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj

# Или удалить все миграции и пересоздать БД (ОСТОРОЖНО: удалит все данные!)
dotnet ef database drop --project src/InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj --startup-project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj
dotnet ef database update --project src/InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj --startup-project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj
```

### Шаг 2: Запуск веб-приложения

**Вариант 1: Из корневой папки проекта**

```bash
# Перейдите в корневую папку проекта
cd D:\ForVLGU\KURSOVIK_TP\Kursovik_TP

# Запустите веб-приложение
dotnet run --project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj
```

**Вариант 2: Из папки Web**

```bash
# Перейдите в папку Web
cd src/InsuranceAgency.Web

# Запустите приложение
dotnet run
```

После запуска приложение будет доступно по адресам:

- HTTP: `http://localhost:5000` (или другой порт, указанный в выводе)
- HTTPS: `https://localhost:5001` (или другой порт, указанный в выводе)
- Swagger UI: `https://localhost:5001/swagger` (в режиме Development)

### Шаг 3: Запуск Worker (фоновые задачи) - опционально

Worker можно запустить в отдельном окне командной строки:

```bash
# Из корня проекта
dotnet run --project src/InsuranceAgency.Worker/InsuranceAgency.Worker.csproj

# Или из папки Worker
cd src/InsuranceAgency.Worker
dotnet run
```

### Запуск тестов

```bash
# Из корня проекта
dotnet test src/InsuranceAgency.Tests/InsuranceAgency.Tests.csproj

# Или запустить все тесты
dotnet test
```

## Быстрый старт (краткая инструкция)

Если вы уже настроили проект ранее и просто хотите запустить его:

```bash
# 1. Применить миграции (если БД еще не создана)
dotnet ef database update --project src/InsuranceAgency.Infrastructure/InsuranceAgency.Infrastructure.csproj --startup-project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj

# 2. Запустить приложение
dotnet run --project src/InsuranceAgency.Web/InsuranceAgency.Web.csproj
```

Откройте браузер и перейдите по адресу, указанному в выводе команды.

## База данных

Проект использует SQL Server LocalDB по умолчанию. Строка подключения настраивается в `appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=InsuranceAgencyDb;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}
```

## API Endpoints

### Аутентификация

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему (JWT токен)
- `GET /api/auth/me` - Получить информацию о текущем пользователе
- `PUT /api/auth/me` - Обновить профиль текущего пользователя
- `DELETE /api/auth/me` - Удалить аккаунт текущего пользователя

### Клиенты (требуется авторизация: Agent, Administrator)

- `GET /api/clients` - Получить всех клиентов
- `GET /api/clients/{id}` - Получить клиента по ID
- `POST /api/clients` - Создать нового клиента
- `PUT /api/clients/{id}` - Обновить данные клиента
- `DELETE /api/clients/{id}` - Удалить клиента

### Договоры (требуется авторизация)

- `GET /api/contracts` - Получить все договоры (для всех авторизованных)
- `GET /api/contracts/{id}` - Получить договор по ID
- `POST /api/contracts` - Создать новый договор (Agent, Administrator)
- `POST /api/contracts/{id}/register` - Зарегистрировать договор (Agent, Administrator)

### Платежи (требуется авторизация)

- `POST /api/payments` - Инициировать платеж
- `GET /api/payments` - Получить все платежи

### Услуги (требуется авторизация)

- `GET /api/services` - Получить все страховые услуги
- `GET /api/services/{id}` - Получить услугу по ID
- `POST /api/services` - Создать новую услугу (Administrator)
- `PUT /api/services/{id}` - Обновить услугу (Administrator)
- `DELETE /api/services/{id}` - Удалить услугу (Administrator)

## MVC Страницы

### Публичные страницы

- `/` - Главная страница с новостями и информацией
- `/Account/Login` - Страница входа
- `/Account/Register` - Страница регистрации

### Защищенные страницы (требуется авторизация)

- `/Dashboard` - Личная панель управления (зависит от роли)

#### Для Агента и Администратора:

- `/Agent/Clients` - Справочник клиентов
- `/Agent/Contracts` - Список договоров
- `/Agent/SearchContracts` - Поиск договоров
- `/Agent/ClientHistory/{id}` - История операций клиента

#### Для Клиента:

- `/Client/Profile` - Управление профилем
- `/Client/PayContract/{id}` - Оплата договора
- `/Client/History` - История операций
- `/Client/CreateApplication` - Создание заявки на договор

#### Для Администратора:

- `/Admin/Services` - Управление услугами страхования

## Платежная система

**Важно:** Платежная система реализована как **заглушка (MockPaymentGateway)**. Она не выполняет реальных платежей и всегда возвращает успешный результат с мнимым transactionId. Это сделано для целей курсовой работы и тестирования.

## Архитектура

Проект следует принципам:

- **Separation of Concerns** - разделение на слои
- **Dependency Inversion** - зависимости направлены внутрь
- **Repository Pattern** - абстракция доступа к данным
- **Service Layer** - бизнес-логика в сервисах
- **DTO Pattern** - передача данных через DTO
- **Value Objects** - Money, DateRange
- **Domain Events** - события домена

## Тестирование

Проект включает:

- **Unit тесты** - тестирование сервисов и доменной логики
- **Integration тесты** - тестирование API и работы с БД

## Модульная структура

Проект разделен на два основных модуля согласно требованиям:

### Модуль 1: Клиенты

- **Расположение**: `src/InsuranceAgency.Domain/Entities/Client.cs`, `src/InsuranceAgency.Web/Controllers/ClientsController.cs`
- **Функциональность**: Управление клиентской базой, профилями, верификация документов, история операций

### Модуль 2: Заказы/Договоры

- **Расположение**: `src/InsuranceAgency.Domain/Entities/Contract.cs`, `src/InsuranceAgency.Web/Controllers/ContractsController.cs`
- **Функциональность**: Управление договорами, заявками, платежами, регистрация договоров

## Автор

Курсовая работа выполнена в рамках семестра по дисциплине "Технологии программирования".

## Предупреждения о безопасности

При сборке проекта (`dotnet build`) вы можете увидеть предупреждения о известных уязвимостях в пакетах NuGet (NU1902, NU1903). Это нормально для учебного проекта:

- Предупреждения не критичны и не препятствуют работе приложения
- Большинство уязвимостей связаны с транзитивными зависимостями
- Для продакшн-приложений рекомендуется регулярно обновлять пакеты
